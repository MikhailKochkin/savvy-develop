# Migration `20201125140615-init`

This migration has been generated by MikhailKochkin at 11/25/2020, 5:06:15 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TYPE "public"."CheckType" AS ENUM ('WORD', 'IDEA')

CREATE TYPE "public"."CourseType" AS ENUM ('PUBLIC', 'PRIVATE', 'FORMONEY', 'UNI', 'CHALLENGE')

CREATE TYPE "public"."Level" AS ENUM ('STANDARD', 'ADVANCED')

CREATE TYPE "public"."Permission" AS ENUM ('ADMIN', 'USER')

CREATE TYPE "public"."Status" AS ENUM ('STUDENT', 'LAWYER', 'AUTHOR', 'SAVVY_AUTHOR', 'HR')

CREATE TYPE "public"."TaskType" AS ENUM ('TEST', 'FORM')

CREATE TYPE "public"."Type" AS ENUM ('STORY', 'REGULAR', 'CHALLENGE')

CREATE TABLE "Application" (
    "id" TEXT NOT NULL,
    "applicantId" TEXT NOT NULL,
    "applicantName" TEXT NOT NULL,
    "message" TEXT,
    "coursePageID" TEXT NOT NULL,
    "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updatedAt" TIMESTAMP(3) NOT NULL,
    "coursePageId" TEXT NOT NULL,

    PRIMARY KEY ("id")
)

CREATE TABLE "CareerTrack" (
    "id" TEXT NOT NULL,
    "name" TEXT NOT NULL,
    "img" TEXT,
    "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updatedAt" TIMESTAMP(3) NOT NULL,

    PRIMARY KEY ("id")
)

CREATE TABLE "CareerTrackUnit" (
    "id" TEXT NOT NULL,
    "title" TEXT NOT NULL,
    "number" INTEGER,
    "img" TEXT,
    "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updatedAt" TIMESTAMP(3) NOT NULL,
    "topics" TEXT[],
    "articles" TEXT[],
    "careerTrackId" TEXT NOT NULL,

    PRIMARY KEY ("id")
)

CREATE TABLE "ChallengeResult" (
    "id" TEXT NOT NULL,
    "correct" INTEGER,
    "wrong" INTEGER,
    "time" INTEGER,
    "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updatedAt" TIMESTAMP(3) NOT NULL,
    "studentId" TEXT NOT NULL,
    "lessonId" TEXT NOT NULL,

    PRIMARY KEY ("id")
)

CREATE TABLE "Clause" (
    "id" TEXT NOT NULL,
    "number" INTEGER NOT NULL,
    "commentary" TEXT NOT NULL,
    "sample" TEXT,
    "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updatedAt" TIMESTAMP(3) NOT NULL,
    "keywords" TEXT[],
    "userId" TEXT NOT NULL,
    "documentId" TEXT NOT NULL,

    PRIMARY KEY ("id")
)

CREATE TABLE "Company" (
    "id" TEXT NOT NULL,
    "name" TEXT NOT NULL,
    "paidMonths" INTEGER DEFAULT 0,
    "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updatedAt" TIMESTAMP(3) NOT NULL,

    PRIMARY KEY ("id")
)

CREATE TABLE "Construction" (
    "id" TEXT NOT NULL,
    "name" TEXT NOT NULL,
    "hint" TEXT,
    "type" TEXT NOT NULL,
    "lessonID" TEXT NOT NULL,
    "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updatedAt" TIMESTAMP(3) NOT NULL,
    "variants" TEXT[],
    "answer" TEXT[],
    "userId" TEXT NOT NULL,
    "lessonId" TEXT NOT NULL,

    PRIMARY KEY ("id")
)

CREATE TABLE "ConstructionResult" (
    "id" TEXT NOT NULL,
    "answer" TEXT,
    "attempts" INTEGER,
    "constructionID" TEXT,
    "lessonID" TEXT,
    "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updatedAt" TIMESTAMP(3) NOT NULL,
    "inputs" TEXT[],
    "studentId" TEXT NOT NULL,
    "lessonId" TEXT,
    "constructionId" TEXT,

    PRIMARY KEY ("id")
)

CREATE TABLE "CoursePage" (
    "id" TEXT NOT NULL,
    "title" TEXT NOT NULL,
    "description" TEXT NOT NULL,
    "image" TEXT NOT NULL,
    "banner" TEXT,
    "news" TEXT DEFAULT E'Добро пожаловать на курс! Проходите уроки, выполняйте задания и выполняйте финальное задание!',
    "audience" TEXT,
    "result" TEXT,
    "tariffs" TEXT,
    "methods" TEXT,
    "video" TEXT,
    "reviews" JSONB,
    "numInCareerTrack" INTEGER,
    "published" BOOLEAN DEFAULT false,
    "uniID" TEXT,
    "courseType" "CourseType",
    "price" INTEGER,
    "discountPrice" INTEGER,
    "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updatedAt" TIMESTAMP(3) NOT NULL,
    "company" TEXT,
    "weeks" INTEGER,
    "subscription" BOOLEAN DEFAULT false,
    "subscriptionPrice" INTEGER,
    "batch" TEXT,
    "openLesson" TEXT,
    "tags" TEXT[],
    "students" TEXT[],
    "promocode" JSONB[],
    "userId" TEXT NOT NULL,
    "uniId" TEXT,

    PRIMARY KEY ("id")
)

CREATE TABLE "CourseVisit" (
    "id" TEXT NOT NULL,
    "visitsNumber" INTEGER DEFAULT 0,
    "finish" TIMESTAMP(3),
    "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updatedAt" TIMESTAMP(3) NOT NULL,
    "reminders" TIMESTAMP(3)[],
    "studentId" TEXT NOT NULL,
    "coursePageId" TEXT NOT NULL,

    PRIMARY KEY ("id")
)

CREATE TABLE "Document" (
    "id" TEXT NOT NULL,
    "title" TEXT NOT NULL,
    "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updatedAt" TIMESTAMP(3) NOT NULL,
    "userId" TEXT NOT NULL,
    "lessonId" TEXT NOT NULL,

    PRIMARY KEY ("id")
)

CREATE TABLE "DocumentResult" (
    "id" TEXT NOT NULL,
    "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updatedAt" TIMESTAMP(3) NOT NULL,
    "answers" TEXT[],
    "drafts" TEXT[],
    "userId" TEXT,
    "lessonId" TEXT,
    "documentId" TEXT,

    PRIMARY KEY ("id")
)

CREATE TABLE "Feedback" (
    "id" TEXT NOT NULL,
    "text" TEXT NOT NULL,
    "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updatedAt" TIMESTAMP(3) NOT NULL,
    "teacherId" TEXT NOT NULL,
    "studentId" TEXT NOT NULL,
    "lessonId" TEXT,

    PRIMARY KEY ("id")
)

CREATE TABLE "Forum" (
    "id" TEXT NOT NULL,
    "text" TEXT,
    "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updatedAt" TIMESTAMP(3) NOT NULL,
    "userId" TEXT,

    PRIMARY KEY ("id")
)

CREATE TABLE "LegalPortfolio" (
    "id" TEXT NOT NULL,
    "cv" TEXT,
    "resume" TEXT,
    "open" BOOLEAN NOT NULL DEFAULT false,
    "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updatedAt" TIMESTAMP(3) NOT NULL,

    PRIMARY KEY ("id")
)

CREATE TABLE "Lesson" (
    "id" TEXT NOT NULL,
    "name" TEXT NOT NULL,
    "number" INTEGER,
    "text" TEXT NOT NULL,
    "type" TEXT DEFAULT E'REGULAR',
    "description" TEXT,
    "structure" JSONB,
    "open" BOOLEAN DEFAULT false,
    "coursePageID" TEXT NOT NULL,
    "change" TEXT,
    "challenge_num" INTEGER DEFAULT 10,
    "published" BOOLEAN DEFAULT false,
    "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updatedAt" TIMESTAMP(3) NOT NULL,
    "map" JSONB[],
    "userId" TEXT NOT NULL,
    "coursePageId" TEXT NOT NULL,
    "forumId" TEXT,

    PRIMARY KEY ("id")
)

CREATE TABLE "LessonResult" (
    "id" TEXT NOT NULL,
    "visitsNumber" INTEGER DEFAULT 0,
    "lessonID" TEXT,
    "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updatedAt" TIMESTAMP(3) NOT NULL,
    "progress" INTEGER DEFAULT 0,
    "studentId" TEXT NOT NULL,
    "lessonId" TEXT NOT NULL,

    PRIMARY KEY ("id")
)

CREATE TABLE "NewTest" (
    "id" TEXT NOT NULL,
    "ifRight" TEXT,
    "ifWrong" TEXT,
    "type" TEXT DEFAULT E'TEST',
    "lessonID" TEXT,
    "next" JSONB,
    "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updatedAt" TIMESTAMP(3) NOT NULL,
    "question" TEXT[],
    "answers" TEXT[],
    "correct" BOOLEAN[],
    "userId" TEXT NOT NULL,
    "lessonId" TEXT,

    PRIMARY KEY ("id")
)

CREATE TABLE "Note" (
    "id" TEXT NOT NULL,
    "text" TEXT NOT NULL,
    "next" JSONB,
    "lessonID" TEXT,
    "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updatedAt" TIMESTAMP(3) NOT NULL,
    "userId" TEXT,
    "lessonId" TEXT,

    PRIMARY KEY ("id")
)

CREATE TABLE "Order" (
    "id" TEXT NOT NULL,
    "paymentID" TEXT,
    "price" INTEGER,
    "level" "Level",
    "comment" TEXT,
    "promocode" TEXT,
    "isPaid" BOOLEAN,
    "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updatedAt" TIMESTAMP(3) NOT NULL,
    "userId" TEXT NOT NULL,
    "coursePageId" TEXT NOT NULL,

    PRIMARY KEY ("id")
)

CREATE TABLE "Post" (
    "id" TEXT NOT NULL,
    "title" TEXT,
    "text" TEXT,
    "likes" INTEGER DEFAULT 1,
    "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updatedAt" TIMESTAMP(3) NOT NULL,
    "userId" TEXT NOT NULL,
    "tags" TEXT[],

    PRIMARY KEY ("id")
)

CREATE TABLE "Problem" (
    "id" TEXT NOT NULL,
    "text" TEXT NOT NULL,
    "hints" TEXT,
    "solution" TEXT,
    "answer" TEXT,
    "lessonID" TEXT NOT NULL,
    "nodeType" TEXT,
    "nodeID" TEXT,
    "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updatedAt" TIMESTAMP(3) NOT NULL,
    "hintsList" TEXT[],
    "solutionList" TEXT[],
    "userId" TEXT NOT NULL,
    "lessonId" TEXT NOT NULL,

    PRIMARY KEY ("id")
)

CREATE TABLE "ProblemResult" (
    "id" TEXT NOT NULL,
    "answer" TEXT,
    "lessonID" TEXT,
    "problemID" TEXT,
    "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updatedAt" TIMESTAMP(3) NOT NULL,
    "revealed" TEXT[],
    "studentId" TEXT NOT NULL,
    "lessonId" TEXT,
    "problemId" TEXT,

    PRIMARY KEY ("id")
)

CREATE TABLE "Quiz" (
    "id" TEXT NOT NULL,
    "question" TEXT NOT NULL,
    "answer" TEXT NOT NULL,
    "type" TEXT DEFAULT E'TEST',
    "ifRight" TEXT,
    "ifWrong" TEXT,
    "lessonID" TEXT,
    "next" JSONB,
    "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updatedAt" TIMESTAMP(3) NOT NULL,
    "check" TEXT DEFAULT E'IDEA',
    "userId" TEXT NOT NULL,
    "lessonId" TEXT,

    PRIMARY KEY ("id")
)

CREATE TABLE "QuizResult" (
    "id" TEXT NOT NULL,
    "answer" TEXT,
    "attempts" INTEGER,
    "correct" BOOLEAN,
    "lessonID" TEXT,
    "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updatedAt" TIMESTAMP(3) NOT NULL,
    "studentId" TEXT NOT NULL,
    "lessonId" TEXT,
    "quizId" TEXT,

    PRIMARY KEY ("id")
)

CREATE TABLE "Rating" (
    "id" TEXT NOT NULL,
    "rating" INTEGER,
    "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updatedAt" TIMESTAMP(3) NOT NULL,
    "userId" TEXT,
    "forumId" TEXT,

    PRIMARY KEY ("id")
)

CREATE TABLE "Shot" (
    "id" TEXT NOT NULL,
    "title" TEXT NOT NULL,
    "lessonID" TEXT,
    "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updatedAt" TIMESTAMP(3) NOT NULL,
    "parts" TEXT[],
    "comments" TEXT[],
    "userId" TEXT NOT NULL,
    "lessonId" TEXT,

    PRIMARY KEY ("id")
)

CREATE TABLE "ShotResult" (
    "id" TEXT NOT NULL,
    "answer" TEXT NOT NULL,
    "lessonID" TEXT,
    "shotID" TEXT,
    "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updatedAt" TIMESTAMP(3) NOT NULL,
    "studentId" TEXT NOT NULL,
    "lessonId" TEXT,
    "shotId" TEXT,

    PRIMARY KEY ("id")
)

CREATE TABLE "Statement" (
    "id" TEXT NOT NULL,
    "text" TEXT,
    "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updatedAt" TIMESTAMP(3) NOT NULL,
    "userId" TEXT,
    "forumId" TEXT,

    PRIMARY KEY ("id")
)

CREATE TABLE "TestResult" (
    "id" TEXT NOT NULL,
    "answer" TEXT,
    "attempts" INTEGER,
    "lessonID" TEXT,
    "testID" TEXT,
    "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updatedAt" TIMESTAMP(3) NOT NULL,
    "studentId" TEXT NOT NULL,
    "lessonId" TEXT,
    "testId" TEXT,

    PRIMARY KEY ("id")
)

CREATE TABLE "TextEditor" (
    "id" TEXT NOT NULL,
    "name" TEXT NOT NULL,
    "text" TEXT NOT NULL,
    "totalMistakes" INTEGER,
    "lessonID" TEXT NOT NULL,
    "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updatedAt" TIMESTAMP(3) NOT NULL,
    "userId" TEXT NOT NULL,
    "lessonId" TEXT NOT NULL,

    PRIMARY KEY ("id")
)

CREATE TABLE "TextEditorResult" (
    "id" TEXT NOT NULL,
    "attempts" INTEGER,
    "wrong" TEXT NOT NULL,
    "correct" TEXT NOT NULL,
    "result" BOOLEAN,
    "guess" TEXT NOT NULL,
    "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updatedAt" TIMESTAMP(3) NOT NULL,
    "studentId" TEXT NOT NULL,
    "lessonId" TEXT,
    "textEditorId" TEXT,

    PRIMARY KEY ("id")
)

CREATE TABLE "Uni" (
    "id" TEXT NOT NULL,
    "title" TEXT NOT NULL,
    "capacity" INTEGER DEFAULT 2,
    "paidMonths" INTEGER DEFAULT 0,
    "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updatedAt" TIMESTAMP(3) NOT NULL,

    PRIMARY KEY ("id")
)

CREATE TABLE "User" (
    "id" TEXT NOT NULL,
    "name" TEXT NOT NULL,
    "surname" TEXT,
    "email" TEXT NOT NULL,
    "password" TEXT NOT NULL,
    "resetToken" TEXT,
    "resetTokenExpiry" DECIMAL(65,30),
    "status" TEXT DEFAULT E'STUDENT',
    "uniID" TEXT,
    "description" TEXT,
    "resume" TEXT,
    "coverLetter" TEXT,
    "careerTrackID" TEXT,
    "isFamiliar" BOOLEAN NOT NULL DEFAULT false,
    "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updatedAt" TIMESTAMP(3) NOT NULL,
    "image" TEXT,
    "favourites" TEXT[],
    "subjects" TEXT[],
    "interests" TEXT[],
    "permissions" "Permission"[],
    "uniId" TEXT,
    "companyId" TEXT,
    "legalPortfolioId" TEXT,
    "careerTrackId" TEXT,
    "levelId" TEXT,

    PRIMARY KEY ("id")
)

CREATE TABLE "UserLevel" (
    "id" TEXT NOT NULL,
    "level" DECIMAL(65,30) DEFAULT 1,
    "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updatedAt" TIMESTAMP(3) NOT NULL,

    PRIMARY KEY ("id")
)

CREATE TABLE "_CoursePageForCareer" (
    "A" TEXT NOT NULL,
    "B" TEXT NOT NULL
)

CREATE TABLE "_CareerTrackUnitToCoursePage" (
    "A" TEXT NOT NULL,
    "B" TEXT NOT NULL
)

CREATE TABLE "_AuthorsCoursePage" (
    "A" TEXT NOT NULL,
    "B" TEXT NOT NULL
)

CREATE TABLE "_CoursePageToCoursePage" (
    "A" TEXT NOT NULL,
    "B" TEXT NOT NULL
)

CREATE TABLE "_PostCoursePages" (
    "A" TEXT NOT NULL,
    "B" TEXT NOT NULL
)

CREATE TABLE "_UserSubjects" (
    "A" TEXT NOT NULL,
    "B" TEXT NOT NULL
)

CREATE UNIQUE INDEX "Lesson.forumId_unique" ON "Lesson"("forumId")

CREATE UNIQUE INDEX "User.email_unique" ON "User"("email")

CREATE UNIQUE INDEX "User.legalPortfolioId_unique" ON "User"("legalPortfolioId")

CREATE UNIQUE INDEX "User.careerTrackId_unique" ON "User"("careerTrackId")

CREATE UNIQUE INDEX "User.levelId_unique" ON "User"("levelId")

CREATE UNIQUE INDEX "_CoursePageForCareer_AB_unique" ON "_CoursePageForCareer"("A", "B")

CREATE INDEX "_CoursePageForCareer_B_index" ON "_CoursePageForCareer"("B")

CREATE UNIQUE INDEX "_CareerTrackUnitToCoursePage_AB_unique" ON "_CareerTrackUnitToCoursePage"("A", "B")

CREATE INDEX "_CareerTrackUnitToCoursePage_B_index" ON "_CareerTrackUnitToCoursePage"("B")

CREATE UNIQUE INDEX "_AuthorsCoursePage_AB_unique" ON "_AuthorsCoursePage"("A", "B")

CREATE INDEX "_AuthorsCoursePage_B_index" ON "_AuthorsCoursePage"("B")

CREATE UNIQUE INDEX "_CoursePageToCoursePage_AB_unique" ON "_CoursePageToCoursePage"("A", "B")

CREATE INDEX "_CoursePageToCoursePage_B_index" ON "_CoursePageToCoursePage"("B")

CREATE UNIQUE INDEX "_PostCoursePages_AB_unique" ON "_PostCoursePages"("A", "B")

CREATE INDEX "_PostCoursePages_B_index" ON "_PostCoursePages"("B")

CREATE UNIQUE INDEX "_UserSubjects_AB_unique" ON "_UserSubjects"("A", "B")

CREATE INDEX "_UserSubjects_B_index" ON "_UserSubjects"("B")

ALTER TABLE "Application" ADD FOREIGN KEY("coursePageId")REFERENCES "CoursePage"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "CareerTrackUnit" ADD FOREIGN KEY("careerTrackId")REFERENCES "CareerTrack"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "ChallengeResult" ADD FOREIGN KEY("lessonId")REFERENCES "Lesson"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "ChallengeResult" ADD FOREIGN KEY("studentId")REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "Clause" ADD FOREIGN KEY("documentId")REFERENCES "Document"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "Clause" ADD FOREIGN KEY("userId")REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "Construction" ADD FOREIGN KEY("lessonId")REFERENCES "Lesson"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "Construction" ADD FOREIGN KEY("userId")REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "ConstructionResult" ADD FOREIGN KEY("constructionId")REFERENCES "Construction"("id") ON DELETE SET NULL ON UPDATE CASCADE

ALTER TABLE "ConstructionResult" ADD FOREIGN KEY("lessonId")REFERENCES "Lesson"("id") ON DELETE SET NULL ON UPDATE CASCADE

ALTER TABLE "ConstructionResult" ADD FOREIGN KEY("studentId")REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "CoursePage" ADD FOREIGN KEY("company")REFERENCES "Company"("id") ON DELETE SET NULL ON UPDATE CASCADE

ALTER TABLE "CoursePage" ADD FOREIGN KEY("uniId")REFERENCES "Uni"("id") ON DELETE SET NULL ON UPDATE CASCADE

ALTER TABLE "CoursePage" ADD FOREIGN KEY("userId")REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "CourseVisit" ADD FOREIGN KEY("coursePageId")REFERENCES "CoursePage"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "CourseVisit" ADD FOREIGN KEY("studentId")REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "Document" ADD FOREIGN KEY("lessonId")REFERENCES "Lesson"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "Document" ADD FOREIGN KEY("userId")REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "DocumentResult" ADD FOREIGN KEY("documentId")REFERENCES "Document"("id") ON DELETE SET NULL ON UPDATE CASCADE

ALTER TABLE "DocumentResult" ADD FOREIGN KEY("lessonId")REFERENCES "Lesson"("id") ON DELETE SET NULL ON UPDATE CASCADE

ALTER TABLE "DocumentResult" ADD FOREIGN KEY("userId")REFERENCES "User"("id") ON DELETE SET NULL ON UPDATE CASCADE

ALTER TABLE "Feedback" ADD FOREIGN KEY("lessonId")REFERENCES "Lesson"("id") ON DELETE SET NULL ON UPDATE CASCADE

ALTER TABLE "Feedback" ADD FOREIGN KEY("studentId")REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "Feedback" ADD FOREIGN KEY("teacherId")REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "Forum" ADD FOREIGN KEY("userId")REFERENCES "User"("id") ON DELETE SET NULL ON UPDATE CASCADE

ALTER TABLE "Lesson" ADD FOREIGN KEY("coursePageId")REFERENCES "CoursePage"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "Lesson" ADD FOREIGN KEY("forumId")REFERENCES "Forum"("id") ON DELETE SET NULL ON UPDATE CASCADE

ALTER TABLE "Lesson" ADD FOREIGN KEY("userId")REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "LessonResult" ADD FOREIGN KEY("lessonId")REFERENCES "Lesson"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "LessonResult" ADD FOREIGN KEY("studentId")REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "NewTest" ADD FOREIGN KEY("lessonId")REFERENCES "Lesson"("id") ON DELETE SET NULL ON UPDATE CASCADE

ALTER TABLE "NewTest" ADD FOREIGN KEY("userId")REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "Note" ADD FOREIGN KEY("lessonId")REFERENCES "Lesson"("id") ON DELETE SET NULL ON UPDATE CASCADE

ALTER TABLE "Note" ADD FOREIGN KEY("userId")REFERENCES "User"("id") ON DELETE SET NULL ON UPDATE CASCADE

ALTER TABLE "Order" ADD FOREIGN KEY("coursePageId")REFERENCES "CoursePage"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "Order" ADD FOREIGN KEY("userId")REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "Post" ADD FOREIGN KEY("userId")REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "Problem" ADD FOREIGN KEY("lessonId")REFERENCES "Lesson"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "Problem" ADD FOREIGN KEY("userId")REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "ProblemResult" ADD FOREIGN KEY("lessonId")REFERENCES "Lesson"("id") ON DELETE SET NULL ON UPDATE CASCADE

ALTER TABLE "ProblemResult" ADD FOREIGN KEY("problemId")REFERENCES "Problem"("id") ON DELETE SET NULL ON UPDATE CASCADE

ALTER TABLE "ProblemResult" ADD FOREIGN KEY("studentId")REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "Quiz" ADD FOREIGN KEY("lessonId")REFERENCES "Lesson"("id") ON DELETE SET NULL ON UPDATE CASCADE

ALTER TABLE "Quiz" ADD FOREIGN KEY("userId")REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "QuizResult" ADD FOREIGN KEY("lessonId")REFERENCES "Lesson"("id") ON DELETE SET NULL ON UPDATE CASCADE

ALTER TABLE "QuizResult" ADD FOREIGN KEY("quizId")REFERENCES "Quiz"("id") ON DELETE SET NULL ON UPDATE CASCADE

ALTER TABLE "QuizResult" ADD FOREIGN KEY("studentId")REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "Rating" ADD FOREIGN KEY("forumId")REFERENCES "Forum"("id") ON DELETE SET NULL ON UPDATE CASCADE

ALTER TABLE "Rating" ADD FOREIGN KEY("userId")REFERENCES "User"("id") ON DELETE SET NULL ON UPDATE CASCADE

ALTER TABLE "Shot" ADD FOREIGN KEY("lessonId")REFERENCES "Lesson"("id") ON DELETE SET NULL ON UPDATE CASCADE

ALTER TABLE "Shot" ADD FOREIGN KEY("userId")REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "ShotResult" ADD FOREIGN KEY("lessonId")REFERENCES "Lesson"("id") ON DELETE SET NULL ON UPDATE CASCADE

ALTER TABLE "ShotResult" ADD FOREIGN KEY("shotId")REFERENCES "Shot"("id") ON DELETE SET NULL ON UPDATE CASCADE

ALTER TABLE "ShotResult" ADD FOREIGN KEY("studentId")REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "Statement" ADD FOREIGN KEY("forumId")REFERENCES "Forum"("id") ON DELETE SET NULL ON UPDATE CASCADE

ALTER TABLE "Statement" ADD FOREIGN KEY("userId")REFERENCES "User"("id") ON DELETE SET NULL ON UPDATE CASCADE

ALTER TABLE "TestResult" ADD FOREIGN KEY("lessonId")REFERENCES "Lesson"("id") ON DELETE SET NULL ON UPDATE CASCADE

ALTER TABLE "TestResult" ADD FOREIGN KEY("studentId")REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "TestResult" ADD FOREIGN KEY("testId")REFERENCES "NewTest"("id") ON DELETE SET NULL ON UPDATE CASCADE

ALTER TABLE "TextEditor" ADD FOREIGN KEY("lessonId")REFERENCES "Lesson"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "TextEditor" ADD FOREIGN KEY("userId")REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "TextEditorResult" ADD FOREIGN KEY("lessonId")REFERENCES "Lesson"("id") ON DELETE SET NULL ON UPDATE CASCADE

ALTER TABLE "TextEditorResult" ADD FOREIGN KEY("studentId")REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "TextEditorResult" ADD FOREIGN KEY("textEditorId")REFERENCES "TextEditor"("id") ON DELETE SET NULL ON UPDATE CASCADE

ALTER TABLE "User" ADD FOREIGN KEY("careerTrackId")REFERENCES "CareerTrack"("id") ON DELETE SET NULL ON UPDATE CASCADE

ALTER TABLE "User" ADD FOREIGN KEY("companyId")REFERENCES "Company"("id") ON DELETE SET NULL ON UPDATE CASCADE

ALTER TABLE "User" ADD FOREIGN KEY("legalPortfolioId")REFERENCES "LegalPortfolio"("id") ON DELETE SET NULL ON UPDATE CASCADE

ALTER TABLE "User" ADD FOREIGN KEY("levelId")REFERENCES "UserLevel"("id") ON DELETE SET NULL ON UPDATE CASCADE

ALTER TABLE "User" ADD FOREIGN KEY("uniId")REFERENCES "Uni"("id") ON DELETE SET NULL ON UPDATE CASCADE

ALTER TABLE "_CoursePageForCareer" ADD FOREIGN KEY("A")REFERENCES "CareerTrack"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "_CoursePageForCareer" ADD FOREIGN KEY("B")REFERENCES "CoursePage"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "_CareerTrackUnitToCoursePage" ADD FOREIGN KEY("A")REFERENCES "CareerTrackUnit"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "_CareerTrackUnitToCoursePage" ADD FOREIGN KEY("B")REFERENCES "CoursePage"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "_AuthorsCoursePage" ADD FOREIGN KEY("A")REFERENCES "CoursePage"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "_AuthorsCoursePage" ADD FOREIGN KEY("B")REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "_CoursePageToCoursePage" ADD FOREIGN KEY("A")REFERENCES "CoursePage"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "_CoursePageToCoursePage" ADD FOREIGN KEY("B")REFERENCES "CoursePage"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "_PostCoursePages" ADD FOREIGN KEY("A")REFERENCES "CoursePage"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "_PostCoursePages" ADD FOREIGN KEY("B")REFERENCES "Post"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "_UserSubjects" ADD FOREIGN KEY("A")REFERENCES "CoursePage"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "_UserSubjects" ADD FOREIGN KEY("B")REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201113221021-bro..20201125140615-init
--- datamodel.dml
+++ datamodel.dml
@@ -3,37 +3,649 @@
 }
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
+model Application {
+  id            String     @id @default(cuid())
+  applicantId   String     @default(cuid())
+  applicantName String
+  message       String?
+  coursePageID  String     @default(cuid())
+  createdAt     DateTime   @default(now())
+  updatedAt     DateTime   @updatedAt
+  coursePageId  String
+  coursePage    CoursePage @relation(fields: [coursePageId], references: [id])
+}
+
+model CareerTrack {
+  id               String            @id @default(cuid())
+  name             String
+  img              String?
+  createdAt        DateTime          @default(now())
+  updatedAt        DateTime          @updatedAt
+  careerTrackUnits CareerTrackUnit[]
+  members          User?
+  coursePages      CoursePage[]      @relation("CoursePageForCareer")
+}
+
+model CareerTrackUnit {
+  id            String       @id @default(cuid())
+  title         String
+  number        Int?
+  img           String?
+  createdAt     DateTime     @default(now())
+  updatedAt     DateTime     @updatedAt
+  topics        String[]
+  articles      String[]
+  careerTrackId String
+  careerTrack   CareerTrack  @relation(fields: [careerTrackId], references: [id])
+  coursePages   CoursePage[]
+}
+
+model ChallengeResult {
+  id        String   @id @default(cuid())
+  correct   Int?
+  wrong     Int?
+  time      Int?
+  createdAt DateTime @default(now())
+  updatedAt DateTime @updatedAt
+  studentId String
+  lessonId  String
+  lesson    Lesson   @relation(fields: [lessonId], references: [id])
+  student   User     @relation(fields: [studentId], references: [id])
+}
+
+model Clause {
+  id         String   @id @default(cuid())
+  number     Int
+  commentary String
+  sample     String?
+  createdAt  DateTime @default(now())
+  updatedAt  DateTime @updatedAt
+  keywords   String[]
+  userId     String
+  documentId String
+  document   Document @relation(fields: [documentId], references: [id])
+  user       User     @relation(fields: [userId], references: [id])
+}
+
+model Company {
+  id         String       @id @default(cuid())
+  name       String
+  paidMonths Int?         @default(0)
+  createdAt  DateTime     @default(now())
+  updatedAt  DateTime     @updatedAt
+  CoursePage CoursePage[]
+  hrs        User[]
+}
+
+model Construction {
+  id                  String               @id @default(cuid())
+  name                String
+  hint                String?
+  type                String
+  lessonID            String               @default(cuid())
+  createdAt           DateTime             @default(now())
+  updatedAt           DateTime             @updatedAt
+  variants            String[]
+  answer              String[]
+  userId              String
+  lessonId            String
+  lesson              Lesson               @relation(fields: [lessonId], references: [id])
+  user                User                 @relation(fields: [userId], references: [id])
+  constructionResults ConstructionResult[]
+}
+
+model ConstructionResult {
+  id             String        @id @default(cuid())
+  answer         String?
+  attempts       Int?
+  constructionID String?       @default(cuid())
+  lessonID       String?       @default(cuid())
+  createdAt      DateTime      @default(now())
+  updatedAt      DateTime      @updatedAt
+  inputs         String[]
+  studentId      String
+  lessonId       String?
+  constructionId String?
+  construction   Construction? @relation(fields: [constructionId], references: [id])
+  lesson         Lesson?       @relation(fields: [lessonId], references: [id])
+  student        User          @relation(fields: [studentId], references: [id])
+}
+
+model CoursePage {
+  id                    String            @id @default(cuid())
+  title                 String
+  description           String
+  image                 String
+  banner                String?
+  news                  String?           @default("Добро пожаловать на курс! Проходите уроки, выполняйте задания и выполняйте финальное задание!")
+  audience              String?
+  result                String?
+  tariffs               String?
+  methods               String?
+  video                 String?
+  reviews               Json?
+  numInCareerTrack      Int?
+  published             Boolean?          @default(false)
+  uniID                 String?           @default(cuid())
+  courseType            CourseType?
+  price                 Int?
+  discountPrice         Int?
+  createdAt             DateTime          @default(now())
+  updatedAt             DateTime          @updatedAt
+  company               String?
+  weeks                 Int?
+  subscription          Boolean?          @default(false)
+  subscriptionPrice     Int?
+  batch                 String?
+  openLesson            String?
+  tags                  String[]
+  students              String[]
+  promocode             Json[]
+  userId                String
+  uniId                 String?
+  Company               Company?          @relation(fields: [company], references: [id])
+  uni                   Uni?              @relation(fields: [uniId], references: [id])
+  User_CoursePageToUser User              @relation(fields: [userId], references: [id])
+  applications          Application[]
+  visits                CourseVisit[]
+  lessons               Lesson[]
+  orders                Order[]
+  authors               User[]            @relation("AuthorsCoursePage")
+  careerTrackUnit       CareerTrackUnit[]
+  careerTrack           CareerTrack[]     @relation("CoursePageForCareer")
+  CoursePage_B          CoursePage[]      @relation("CoursePageToCoursePage")
+  CoursePage_A          CoursePage[]      @relation("CoursePageToCoursePage")
+  posts                 Post[]            @relation("PostCoursePages")
+  new_students          User[]            @relation("UserSubjects")
+}
+
+model CourseVisit {
+  id           String     @id @default(cuid())
+  visitsNumber Int?       @default(0)
+  finish       DateTime?
+  createdAt    DateTime   @default(now())
+  updatedAt    DateTime   @updatedAt
+  reminders    DateTime[]
+  studentId    String
+  coursePageId String
+  coursePage   CoursePage @relation(fields: [coursePageId], references: [id])
+  student      User       @relation(fields: [studentId], references: [id])
+}
+
+model Document {
+  id              String           @id @default(cuid())
+  title           String
+  createdAt       DateTime         @default(now())
+  updatedAt       DateTime         @updatedAt
+  userId          String
+  lessonId        String
+  lesson          Lesson           @relation(fields: [lessonId], references: [id])
+  user            User             @relation(fields: [userId], references: [id])
+  clauses         Clause[]
+  documentResults DocumentResult[]
+}
+
+model DocumentResult {
+  id         String    @id @default(cuid())
+  createdAt  DateTime  @default(now())
+  updatedAt  DateTime  @updatedAt
+  answers    String[]
+  drafts     String[]
+  userId     String?
+  lessonId   String?
+  documentId String?
+  document   Document? @relation(fields: [documentId], references: [id])
+  lesson     Lesson?   @relation(fields: [lessonId], references: [id])
+  user       User?     @relation(fields: [userId], references: [id])
+}
+
+model Feedback {
+  id        String   @id @default(cuid())
+  text      String
+  createdAt DateTime @default(now())
+  updatedAt DateTime @updatedAt
+  teacherId String
+  studentId String
+  lessonId  String?
+  lesson    Lesson?  @relation(fields: [lessonId], references: [id])
+  teacher   User     @relation("TeacherFeedback", fields: [studentId], references: [id])
+  student   User     @relation("StudentFeedback", fields: [teacherId], references: [id])
+}
+
+model Forum {
+  id         String      @id @default(cuid())
+  text       String?
+  createdAt  DateTime    @default(now())
+  updatedAt  DateTime    @updatedAt
+  userId     String?
+  user       User?       @relation(fields: [userId], references: [id])
+  lesson     Lesson?
+  rating     Rating[]
+  statements Statement[]
+}
+
+model LegalPortfolio {
+  id        String   @id @default(cuid())
+  cv        String?
+  resume    String?
+  open      Boolean  @default(false)
+  createdAt DateTime @default(now())
+  updatedAt DateTime @updatedAt
+  user      User?
+}
+
+model Lesson {
+  id                  String               @id @default(cuid())
+  name                String
+  number              Int?
+  text                String
+  type                String?              @default("REGULAR")
+  description         String?
+  structure           Json?
+  open                Boolean?             @default(false)
+  coursePageID        String               @default(cuid())
+  change              String?
+  challenge_num       Int?                 @default(10)
+  published           Boolean?             @default(false)
+  createdAt           DateTime             @default(now())
+  updatedAt           DateTime             @updatedAt
+  map                 Json[]
+  userId              String
+  coursePageId        String
+  forumId             String?              @unique
+  coursePage          CoursePage           @relation(fields: [coursePageId], references: [id])
+  forum               Forum?               @relation(fields: [forumId], references: [id])
+  user                User                 @relation(fields: [userId], references: [id])
+  challengeResults    ChallengeResult[]
+  constructions       Construction[]
+  constructionResults ConstructionResult[]
+  documents           Document[]
+  documentResults     DocumentResult[]
+  lessonFeedback      Feedback[]
+  lessonResults       LessonResult[]
+  newTests            NewTest[]
+  notes               Note[]
+  problems            Problem[]
+  problemResults      ProblemResult[]
+  quizes              Quiz[]
+  quizResults         QuizResult[]
+  shots               Shot[]
+  shotResults         ShotResult[]
+  testResults         TestResult[]
+  texteditors         TextEditor[]
+  textEditorResults   TextEditorResult[]
+}
+
+model LessonResult {
+  id           String   @id @default(cuid())
+  visitsNumber Int?     @default(0)
+  lessonID     String?  @default(cuid())
+  createdAt    DateTime @default(now())
+  updatedAt    DateTime @updatedAt
+  progress     Int?     @default(0)
+  studentId    String
+  lessonId     String
+  lesson       Lesson   @relation(fields: [lessonId], references: [id])
+  student      User     @relation(fields: [studentId], references: [id])
+}
+
+model NewTest {
+  id          String       @id @default(cuid())
+  ifRight     String?
+  ifWrong     String?
+  type        String?      @default("TEST")
+  lessonID    String?      @default(cuid())
+  next        Json?
+  createdAt   DateTime     @default(now())
+  updatedAt   DateTime     @updatedAt
+  question    String[]
+  answers     String[]
+  correct     Boolean[]
+  userId      String
+  lessonId    String?
+  lesson      Lesson?      @relation(fields: [lessonId], references: [id])
+  user        User         @relation(fields: [userId], references: [id])
+  testResults TestResult[]
+}
+
+model Note {
+  id        String   @id @default(cuid())
+  text      String
+  next      Json?
+  lessonID  String?  @default(cuid())
+  createdAt DateTime @default(now())
+  updatedAt DateTime @updatedAt
+  userId    String?
+  lessonId  String?
+  lesson    Lesson?  @relation(fields: [lessonId], references: [id])
+  user      User?    @relation(fields: [userId], references: [id])
+}
+
+model Order {
+  id           String     @id @default(cuid())
+  paymentID    String?
+  price        Int?
+  level        Level?
+  comment      String?
+  promocode    String?
+  isPaid       Boolean?
+  createdAt    DateTime   @default(now())
+  updatedAt    DateTime   @updatedAt
+  userId       String
+  coursePageId String
+  coursePage   CoursePage @relation(fields: [coursePageId], references: [id])
+  user         User       @relation(fields: [userId], references: [id])
+}
+
 model Post {
-  id        Int      @id @default(autoincrement())
-  title     String
+  id          String       @id @default(cuid())
+  title       String?
+  text        String?
+  likes       Int?         @default(1)
+  createdAt   DateTime     @default(now())
+  updatedAt   DateTime     @updatedAt
+  userId      String
+  tags        String[]
+  user        User         @relation(fields: [userId], references: [id])
+  coursePages CoursePage[] @relation("PostCoursePages")
+}
+
+model Problem {
+  id             String          @id @default(cuid())
+  text           String
+  hints          String?
+  solution       String?
+  answer         String?
+  lessonID       String          @default(cuid())
+  nodeType       String?
+  nodeID         String?         @default(cuid())
+  createdAt      DateTime        @default(now())
+  updatedAt      DateTime        @updatedAt
+  hintsList      String[]
+  solutionList   String[]
+  userId         String
+  lessonId       String
+  lesson         Lesson          @relation(fields: [lessonId], references: [id])
+  user           User            @relation(fields: [userId], references: [id])
+  problemResults ProblemResult[]
+}
+
+model ProblemResult {
+  id        String   @id @default(cuid())
+  answer    String?
+  lessonID  String?  @default(cuid())
+  problemID String?  @default(cuid())
   createdAt DateTime @default(now())
-  content   String?
-  published Boolean  @default(false)
-  author      User     @relation(fields: [authorId], references: [id])
-  authorId  Int
+  updatedAt DateTime @updatedAt
+  revealed  String[]
+  studentId String
+  lessonId  String?
+  problemId String?
+  lesson    Lesson?  @relation(fields: [lessonId], references: [id])
+  problem   Problem? @relation(fields: [problemId], references: [id])
+  student   User     @relation(fields: [studentId], references: [id])
 }
-model Profile {
-  id     Int     @id @default(autoincrement())
-  bio    String?
-  userId Int     @unique
-  user   User    @relation(fields: [userId], references: [id])
+model Quiz {
+  id          String       @id @default(cuid())
+  question    String
+  answer      String
+  type        String?      @default("TEST")
+  ifRight     String?
+  ifWrong     String?
+  lessonID    String?      @default(cuid())
+  next        Json?
+  createdAt   DateTime     @default(now())
+  updatedAt   DateTime     @updatedAt
+  check       String?      @default("IDEA")
+  userId      String
+  lessonId    String?
+  lesson      Lesson?      @relation(fields: [lessonId], references: [id])
+  user        User         @relation(fields: [userId], references: [id])
+  quizResults QuizResult[]
 }
+model QuizResult {
+  id        String   @id @default(cuid())
+  answer    String?
+  attempts  Int?
+  correct   Boolean?
+  lessonID  String?  @default(cuid())
+  createdAt DateTime @default(now())
+  updatedAt DateTime @updatedAt
+  studentId String
+  lessonId  String?
+  quizId    String?
+  lesson    Lesson?  @relation(fields: [lessonId], references: [id])
+  quiz      Quiz?    @relation(fields: [quizId], references: [id])
+  student   User     @relation(fields: [studentId], references: [id])
+}
+
+model Rating {
+  id        String   @id @default(cuid())
+  rating    Int?
+  createdAt DateTime @default(now())
+  updatedAt DateTime @updatedAt
+  userId    String?
+  forumId   String?
+  forum     Forum?   @relation(fields: [forumId], references: [id])
+  user      User?    @relation(fields: [userId], references: [id])
+}
+
+model Shot {
+  id          String       @id @default(cuid())
+  title       String
+  lessonID    String?      @default(cuid())
+  createdAt   DateTime     @default(now())
+  updatedAt   DateTime     @updatedAt
+  parts       String[]
+  comments    String[]
+  userId      String
+  lessonId    String?
+  lesson      Lesson?      @relation(fields: [lessonId], references: [id])
+  user        User         @relation(fields: [userId], references: [id])
+  shotResults ShotResult[]
+}
+
+model ShotResult {
+  id        String   @id @default(cuid())
+  answer    String
+  lessonID  String?  @default(cuid())
+  shotID    String?  @default(cuid())
+  createdAt DateTime @default(now())
+  updatedAt DateTime @updatedAt
+  studentId String
+  lessonId  String?
+  shotId    String?
+  lesson    Lesson?  @relation(fields: [lessonId], references: [id])
+  shot      Shot?    @relation(fields: [shotId], references: [id])
+  student   User     @relation(fields: [studentId], references: [id])
+}
+
+model Statement {
+  id        String   @id @default(cuid())
+  text      String?
+  createdAt DateTime @default(now())
+  updatedAt DateTime @updatedAt
+  userId    String?
+  forumId   String?
+  forum     Forum?   @relation(fields: [forumId], references: [id])
+  user      User?    @relation(fields: [userId], references: [id])
+}
+
+model TestResult {
+  id        String   @id @default(cuid())
+  answer    String?
+  attempts  Int?
+  lessonID  String?  @default(cuid())
+  testID    String?  @default(cuid())
+  createdAt DateTime @default(now())
+  updatedAt DateTime @updatedAt
+  studentId String
+  lessonId  String?
+  testId    String?
+  lesson    Lesson?  @relation(fields: [lessonId], references: [id])
+  student   User     @relation(fields: [studentId], references: [id])
+  test      NewTest? @relation(fields: [testId], references: [id])
+}
+
+model TextEditor {
+  id                String             @id @default(cuid())
+  name              String
+  text              String
+  totalMistakes     Int?
+  lessonID          String             @default(cuid())
+  createdAt         DateTime           @default(now())
+  updatedAt         DateTime           @updatedAt
+  userId            String
+  lessonId          String
+  lesson            Lesson             @relation(fields: [lessonId], references: [id])
+  user              User               @relation(fields: [userId], references: [id])
+  textEditorResults TextEditorResult[]
+}
+
+model TextEditorResult {
+  id           String      @id @default(cuid())
+  attempts     Int?
+  wrong        String
+  correct      String
+  result       Boolean?
+  guess        String
+  createdAt    DateTime    @default(now())
+  updatedAt    DateTime    @updatedAt
+  studentId    String
+  lessonId     String?
+  textEditorId String?
+  lesson       Lesson?     @relation(fields: [lessonId], references: [id])
+  student      User        @relation(fields: [studentId], references: [id])
+  textEditor   TextEditor? @relation(fields: [textEditorId], references: [id])
+}
+
+model Uni {
+  id             String       @id @default(cuid())
+  title          String
+  capacity       Int?         @default(2)
+  paidMonths     Int?         @default(0)
+  createdAt      DateTime     @default(now())
+  updatedAt      DateTime     @updatedAt
+  uniCoursePages CoursePage[]
+  teachers       User[]
+}
+
 model User {
-  id      Int      @id @default(autoincrement())
-  name    String?
-  email   String   @unique
-  posts    Post[]
-  profile Profile?
+  id                          String               @id @default(cuid())
+  name                        String
+  surname                     String?
+  email                       String               @unique
+  password                    String
+  resetToken                  String?
+  resetTokenExpiry            Float?
+  status                      String?              @default("STUDENT")
+  uniID                       String?              @default(cuid())
+  description                 String?
+  resume                      String?
+  coverLetter                 String?
+  careerTrackID               String?              @default(cuid())
+  isFamiliar                  Boolean              @default(false)
+  createdAt                   DateTime             @default(now())
+  updatedAt                   DateTime             @updatedAt
+  image                       String?
+  favourites                  String[]
+  subjects                    String[]
+  interests                   String[]
+  permissions                 Permission[]
+  uniId                       String?
+  companyId                   String?
+  legalPortfolioId            String?              @unique
+  careerTrackId               String?              @unique
+  levelId                     String?              @unique
+  careerTrack                 CareerTrack?         @relation(fields: [careerTrackId], references: [id])
+  company                     Company?             @relation(fields: [companyId], references: [id])
+  legalPortfolio              LegalPortfolio?      @relation(fields: [legalPortfolioId], references: [id])
+  level                       UserLevel?           @relation(fields: [levelId], references: [id])
+  uni                         Uni?                 @relation(fields: [uniId], references: [id])
+  challengeResults            ChallengeResult[]
+  clauses                     Clause[]
+  construction                Construction[]
+  constructionResults         ConstructionResult[]
+  CoursePage_CoursePageToUser CoursePage[]
+  courseVisits                CourseVisit[]
+  documents                   Document[]
+  documentResults             DocumentResult[]
+  teacherFeedback             Feedback[]           @relation("TeacherFeedback")
+  studentFeedback             Feedback[]           @relation("StudentFeedback")
+  forums                      Forum[]
+  lessons                     Lesson[]
+  lessonResults               LessonResult[]
+  newTests                    NewTest[]
+  notes                       Note[]
+  orders                      Order[]
+  posts                       Post[]
+  problems                    Problem[]
+  problemResults              ProblemResult[]
+  quizes                      Quiz[]
+  quizResults                 QuizResult[]
+  ratings                     Rating[]
+  shots                       Shot[]
+  shotResults                 ShotResult[]
+  statements                  Statement[]
+  testResults                 TestResult[]
+  textEditor                  TextEditor[]
+  textEditorResults           TextEditorResult[]
+  co_coursePages              CoursePage[]         @relation("AuthorsCoursePage")
+  new_subjects                CoursePage[]         @relation("UserSubjects")
 }
-model Bro {
-  id      Int      @id @default(autoincrement())
-  name    String?
-  points   Int  
+model UserLevel {
+  id        String   @id @default(cuid())
+  level     Float?   @default(1)
+  createdAt DateTime @default(now())
+  updatedAt DateTime @updatedAt
+  user      User?
 }
+
+enum CheckType {
+  WORD
+  IDEA
+}
+
+enum CourseType {
+  PUBLIC
+  PRIVATE
+  FORMONEY
+  UNI
+  CHALLENGE
+}
+
+enum Level {
+  STANDARD
+  ADVANCED
+}
+
+enum Permission {
+  ADMIN
+  USER
+}
+
+enum Status {
+  STUDENT
+  LAWYER
+  AUTHOR
+  SAVVY_AUTHOR
+  HR
+}
+
+enum TaskType {
+  TEST
+  FORM
+}
+
+enum Type {
+  STORY
+  REGULAR
+  CHALLENGE
+}
```


