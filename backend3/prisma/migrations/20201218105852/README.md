# Migration `20201218105852`

This migration has been generated by MikhailKochkin at 12/18/2020, 1:58:52 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "Forum" ADD COLUMN     "lessonId" TEXT
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201126124919-minor-bug-fix..20201218105852
--- datamodel.dml
+++ datamodel.dml
@@ -3,9 +3,9 @@
 }
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 model Application {
   id            String     @id @default(cuid())
@@ -169,9 +169,9 @@
   updatedAt    DateTime   @updatedAt
   reminders    DateTime[]
   studentId    String
   coursePageId String
-  coursePages   CoursePage @relation(fields: [coursePageId], references: [id])
+  coursePage   CoursePage @relation(fields: [coursePageId], references: [id])
   student      User       @relation(fields: [studentId], references: [id])
 }
 model Document {
@@ -220,8 +220,9 @@
   createdAt  DateTime    @default(now())
   updatedAt  DateTime    @updatedAt
   userId     String?
   user       User?       @relation(fields: [userId], references: [id])
+  lessonId   String?
   lesson     Lesson?
   rating     Rating[]
   statements Statement[]
 }
@@ -254,9 +255,9 @@
   map                 Json[]
   userId              String
   coursePageId        String
   forumId             String?              @unique
-  coursePages          CoursePage           @relation(fields: [coursePageId], references: [id])
+  coursePage          CoursePage           @relation(fields: [coursePageId], references: [id])
   forum               Forum?               @relation(fields: [forumId], references: [id])
   user                User                 @relation(fields: [userId], references: [id])
   challengeResults    ChallengeResult[]
   constructions       Construction[]
```


