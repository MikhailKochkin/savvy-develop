# Migration `20201126124919-minor-bug-fix`

This migration has been generated by MikhailKochkin at 11/26/2020, 3:49:19 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql

```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201126124553-minor-bugs-fix..20201126124919-minor-bug-fix
--- datamodel.dml
+++ datamodel.dml
@@ -3,9 +3,9 @@
 }
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 model Application {
   id            String     @id @default(cuid())
@@ -76,9 +76,9 @@
   name       String
   paidMonths Int?         @default(0)
   createdAt  DateTime     @default(now())
   updatedAt  DateTime     @updatedAt
-  CoursePage CoursePage[]
+  coursePages CoursePage[]
   hrs        User[]
 }
 model Construction {
@@ -169,9 +169,9 @@
   updatedAt    DateTime   @updatedAt
   reminders    DateTime[]
   studentId    String
   coursePageId String
-  coursePage   CoursePage @relation(fields: [coursePageId], references: [id])
+  coursePages   CoursePage @relation(fields: [coursePageId], references: [id])
   student      User       @relation(fields: [studentId], references: [id])
 }
 model Document {
@@ -254,9 +254,9 @@
   map                 Json[]
   userId              String
   coursePageId        String
   forumId             String?              @unique
-  coursePage          CoursePage           @relation(fields: [coursePageId], references: [id])
+  coursePages          CoursePage           @relation(fields: [coursePageId], references: [id])
   forum               Forum?               @relation(fields: [forumId], references: [id])
   user                User                 @relation(fields: [userId], references: [id])
   challengeResults    ChallengeResult[]
   constructions       Construction[]
```


